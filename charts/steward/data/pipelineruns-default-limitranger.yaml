apiVersion: v1
kind: LimitRange
metadata:
  name: core-resource-limits
spec:
  limits:
    - type: "Container"
      default:
        cpu: 3
        memory: 3Gi
      defaultRequest:
        cpu: "0.5"
        memory: "0.5Gi"
